// AREA OF A RECTANGLE

#include <stdio.h>

int main(){
    int length, breadth;
    printf("Enter the length of rectangle is  \n", length);
    scanf("%d",&length);
    printf("Enter the bradth of rectangle is  \n", breadth);
    scanf("%d",&breadth);


    printf("the area of rectangle is %d \n", length*breadth);
    return 0;
}
